/** 
  * Please copy this file to `config.js`
  * Add your tokens
  * Then run the app
 */

const appConfig = {
	mapboxAccessToken: "your-access-token",
	darkskySecretKey: "your-secret-key"
};

module.exports = appConfig;